/**
 * Library namespace exports
 *
 * These are re-exported libraries that other TSM modules can import.
 * Use the tsm: prefix with namespace:
 *
 *   import { ref, computed } from 'tsm:vue'
 *   import { useRouter } from 'tsm:vue-router'
 */

export * as vue from './vue'
export * as router from './router'
